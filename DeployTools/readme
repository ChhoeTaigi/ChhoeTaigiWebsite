Update process:

1. Git pull from remote/master (Warning: Will do "git reset --hard")
   run "sh gitCheckoutDevelop.sh" or "gitCheckoutMaster.sh"

2. Meteor/npm update
   run "sh prepareMeteor.sh"

3. Meteor build for server
   run "sh buildMeteor.sh"

4. Extract package, install, and restart Passanger
   run "sh deployUpdate.sh"

Done.
